#include <bits/stdc++.h>
using namespace std;

//fungsi untuk mengecek karakter
bool cek(char ch, vector<char> range);

//konversi string ke infix
vector<string> konversiInfix(string, input);

//fungsi untuk menghilangkan spasi
string hapusSpasi(string str);


int main(){
vector<string> kInfix;

string inputan; getline(cin, inputan);

kinfix = konversiInfix(hapusSpasi(inputan));
vector<string>::iterator it = kInfix.begin();

//print
cout << "Print : ";

return 0;
}
