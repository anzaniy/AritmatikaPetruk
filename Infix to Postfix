#include <iostream>
#include <string>
#include <bits/stdc++.h>
using namespace std;

int OrNot(char a){
    if (a == '%' || a == '/' || a == '*' || a == '+' || a == '-' || a == '(')
    return 1;
    return 0;
}
